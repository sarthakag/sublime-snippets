<snippet>
	<content><![CDATA[
vector<int>g[MAXN];
	int n,m;
	cin>>n;
	m =n-1;
	cin>>m;
	for(int i = 0;i<m;i++)
	{
		int u,v;
		cin>>u>>v;
		u--;v--;
		g[u].pb(v);
		g[v].pb(u);
	}
]]></content>
	<!-- Optional: Set a tabTrigger to define how to trigger the snippet -->
	<tabTrigger>_graphInput</tabTrigger>
	<!-- Optional: Set a scope to limit where the snippet will trigger -->
	<!-- <scope>source.python</scope> -->
</snippet>
